# 数据利用与训练执行计划

## 🎯 总体目标

充分利用青鸟系统中的所有数据，进行结构化处理和综合训练，构建完整的AI交易决策系统。

---

## 📊 第一步：数据准备和结构化（优先级：P0）

### 任务1.1: 导入历史信号到数据库

**脚本**: `src/ml_dl/prepare_training_data.py`

**执行命令**:
```bash
python src/ml_dl/prepare_training_data.py
```

**预期结果**:
- 导入100-200个历史信号到`trading_signals`表
- 填充信号的所有字段（时间、价格、规则等）

**检查方法**:
```python
python -c "from db_manager_trader import TraderDBManager; db = TraderDBManager('de'); count = db.get_trading_signals_count(); print(f'信号数量: {count}'); db.close()"
```

### 任务1.2: 评估历史信号

**脚本**: `src/ml_dl/prepare_training_data.py`（包含评估功能）

**执行命令**:
```bash
python src/ml_dl/prepare_training_data.py
```

**预期结果**:
- 创建100-200条`signal_evaluations`记录
- 更新`trading_signals`状态（completed/stopped/missed）
- 计算实际盈亏数据

**检查方法**:
```python
python -c "from db_manager_trader import TraderDBManager; db = TraderDBManager('de'); count = db.get_signal_evaluations_count(); print(f'评估数量: {count}'); db.close()"
```

### 任务1.3: 创建训练数据集

**脚本**: `src/ml_dl/prepare_training_data.py`（包含数据集创建）

**执行命令**:
```bash
python src/ml_dl/prepare_training_data.py
```

**预期结果**:
- 生成`trading_signals/.ml_models/training_dataset.json`
- 包含特征、标签、元数据

---

## 🚀 第二步：综合训练（优先级：P0）

### 任务2.1: 训练信号成功率预测模型

**脚本**: `src/ml_dl/comprehensive_training.py`

**执行命令**:
```bash
python src/ml_dl/comprehensive_training.py
```

**预期结果**:
- 训练随机森林分类器
- 模型准确率>70%
- 保存模型到`.ml_models/`

**检查方法**:
```python
from ml_signal_predictor import MLSignalPredictor
predictor = MLSignalPredictor()
if predictor.load_model():
    print("✓ 模型已加载")
    print(f"模型状态: {predictor.is_trained}")
```

### 任务2.2: 训练价格预测模型（可选）

**脚本**: `src/ml_dl/price_predictor_lstm.py`

**执行命令**:
```bash
python src/ml_dl/price_predictor_lstm.py
```

**预期结果**:
- 训练LSTM模型
- 预测未来1小时价格
- 保存模型

### 任务2.3: 训练RL交易策略（可选）

**脚本**: `src/ml_dl/tensortrade_rl_agent.py`

**执行命令**:
```bash
python src/ml_dl/tensortrade_rl_agent.py
# 选择: 1. 训练新模型
```

**预期结果**:
- 训练PPO模型
- 回测收益率>50%
- 保存模型

---

## 🔄 第三步：系统集成（优先级：P1）

### 任务3.1: 集成ML模型到信号生成

**修改文件**: `src/generate_btc_de_signals.py`

**集成点**: 在生成信号后添加ML评分

**代码示例**:
```python
# 在analyze_timeframe函数中
if self.ml_predictor and self.ml_predictor.is_trained:
    for signal in signals:
        prediction = self.ml_predictor.predict_signal_success(signal)
        signal['ml_score'] = prediction['success_probability']
        signal['ml_confidence'] = prediction['confidence']
```

### 任务3.2: 集成交易员大脑系统

**修改文件**: `src/unified_trading_system.py`

**集成点**: 添加交易员大脑选项

**代码示例**:
```python
class UnifiedTradingSystem:
    def __init__(self, use_trading_brain=False):
        # ... 现有代码 ...
        
        if use_trading_brain:
            from ml_dl.trading_brain_integrated import IntegratedTradingBrain
            self.trading_brain = IntegratedTradingBrain(trader_id='de')
```

---

## 📈 第四步：性能监控和优化（优先级：P2）

### 任务4.1: 模型性能监控

**创建脚本**: `src/ml_dl/model_performance_monitor.py`

**功能**:
- 监控模型预测准确率
- 跟踪信号质量
- 记录模型性能指标

### 任务4.2: 持续优化

**任务**:
- 特征工程优化
- 超参数调优
- 模型集成
- 在线学习

---

## ✅ 执行检查清单

### 数据准备阶段

- [ ] 运行 `prepare_training_data.py`
- [ ] 检查 `trading_signals` 表数据量（预期>100）
- [ ] 检查 `signal_evaluations` 表数据量（预期>100）
- [ ] 检查 `training_dataset.json` 文件是否存在
- [ ] 验证训练数据集样本数量（预期>50）

### 模型训练阶段

- [ ] 运行 `comprehensive_training.py`
- [ ] 检查模型文件是否存在
- [ ] 验证模型准确率（预期>70%）
- [ ] 测试模型预测功能

### 系统集成阶段

- [ ] 集成ML模型到信号生成
- [ ] 测试信号生成流程
- [ ] 验证ML评分功能
- [ ] 检查系统整体性能

---

## 📊 预期成果

### 数据成果

- ✅ 100-200个历史信号导入数据库
- ✅ 100-200条信号评估记录
- ✅ 完整的训练数据集

### 模型成果

- ✅ 信号成功率预测模型（准确率>70%）
- ✅ 价格预测模型（可选）
- ✅ RL交易策略模型（可选）

### 系统成果

- ✅ ML模型集成到信号生成
- ✅ 交易员大脑系统集成
- ✅ 系统性能提升

---

## 🎯 成功标准

### 数据准备成功标准

- [x] `trading_signals` 表有>100条记录
- [x] `signal_evaluations` 表有>100条记录
- [x] `training_dataset.json` 文件存在且有效

### 模型训练成功标准

- [x] 信号成功率预测模型准确率>70%
- [x] 模型文件保存成功
- [x] 模型可以正常加载和预测

### 系统集成成功标准

- [x] ML评分在信号生成中正常工作
- [x] 系统可以生成增强信号
- [x] 系统性能稳定

---

**创建时间**: 2025-12-30  
**预计完成时间**: 数据准备1-2天，训练2-3天，集成1-2天  
**总预计时间**: 4-7天










