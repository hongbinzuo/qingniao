# 电脑休眠/唤醒后使用说明

## 📋 问题：电脑休眠后需要重新启动系统吗？

### 答案：取决于休眠类型

#### ✅ 睡眠模式（Sleep）
- **进程状态**: 进程在内存中保持，继续运行
- **唤醒后**: 无需操作，系统继续运行 ✅
- **建议**: 无需重新启动

#### ⚠️ 休眠模式（Hibernate）
- **进程状态**: 进程被保存到磁盘，唤醒后恢复
- **唤醒后**: 进程会恢复，但建议检查一下
- **建议**: 运行 `abu_check_and_start.bat` 检查

#### ❌ 完全关机/重启
- **进程状态**: 所有进程终止
- **唤醒后**: 需要重新启动系统
- **建议**: 运行 `abu_start_all.bat` 或 `abu_check_and_start.bat`

---

## 🚀 推荐方案：设置开机自动启动

### 一劳永逸的解决方案

设置后，无论电脑如何重启或唤醒，系统都会自动检查并启动。

### 设置步骤

1. **以管理员身份运行PowerShell**

2. **设置开机自动启动**:
   ```batch
   # 以管理员身份运行
   双击运行: setup_abu_auto_start.bat
   ```
   或者使用PowerShell:
   ```powershell
   cd C:\Users\zuoho\code\qingniao
   powershell -ExecutionPolicy Bypass -File "scripts\setup_abu_auto_start.ps1"
   ```

3. **设置每日报告自动生成**:
   ```batch
   # 以管理员身份运行
   双击运行: setup_daily_report.bat
   ```
   或者使用PowerShell:
   ```powershell
   powershell -ExecutionPolicy Bypass -File "scripts\setup_daily_report_task.ps1"
   ```

### 设置后的效果

- ✅ **电脑重启后**: 自动启动ABU系统
- ✅ **电脑休眠唤醒后**: 自动检查并启动（如果未运行）
- ✅ **无需手动操作**: 完全自动化

---

## 🔍 手动检查方案

### 如果不确定系统是否在运行

**运行检查脚本**:
```batch
双击运行: abu_check_and_start.bat
```

**这个脚本会**:
- 检查模式匹配系统是否在运行
- 检查视觉匹配系统是否在运行
- 如果未运行，自动启动
- 如果已运行，跳过启动

---

## 📊 不同场景的处理方式

### 场景1: 电脑睡眠后唤醒

**操作**: 无需操作 ✅

**原因**: 睡眠模式下，进程在内存中保持，唤醒后继续运行

### 场景2: 电脑休眠后唤醒

**操作**: 建议运行 `abu_check_and_start.bat` 检查

**原因**: 休眠模式下，进程会恢复，但可能有些进程没有正确恢复

### 场景3: 电脑重启后

**操作**: 
- **如果设置了自动启动**: 无需操作 ✅
- **如果没有设置**: 运行 `abu_start_all.bat` 或 `abu_check_and_start.bat`

### 场景4: 不确定系统是否在运行

**操作**: 运行 `abu_check_and_start.bat`

**好处**: 自动检查并启动，不会重复启动已运行的进程

---

## 🎯 最佳实践

### 推荐配置

1. **设置开机自动启动**:
   ```powershell
   .\scripts\setup_abu_auto_start.ps1
   ```

2. **设置每日报告自动生成**:
   ```powershell
   .\scripts\setup_daily_report_task.ps1
   ```

3. **日常使用**:
   - 电脑休眠/唤醒：无需操作 ✅
   - 电脑重启：自动启动 ✅
   - 不确定时：运行 `abu_check_and_start.bat` 检查

### 不推荐的做法

- ❌ 每次唤醒都手动运行启动脚本（如果设置了自动启动，这是多余的）
- ❌ 关闭启动的窗口（系统需要这些窗口运行）

---

## 🔧 故障排查

### 如果自动启动没有工作

1. **检查任务计划**:
   ```powershell
   Get-ScheduledTask -TaskName ABU_Auto_Start
   ```

2. **手动运行检查脚本**:
   ```batch
   abu_check_and_start.bat
   ```

3. **查看任务计划日志**:
   - 打开"任务计划程序"
   - 找到 `ABU_Auto_Start` 任务
   - 查看"历史记录"标签

---

## 📝 总结

### 快速答案

**如果设置了开机自动启动**:
- ✅ 电脑休眠/唤醒后：无需操作，系统自动检查并启动
- ✅ 电脑重启后：自动启动

**如果没有设置自动启动**:
- ✅ 睡眠模式：无需操作
- ⚠️ 休眠模式：建议运行 `abu_check_and_start.bat` 检查
- ❌ 完全关机/重启：需要运行 `abu_start_all.bat` 或 `abu_check_and_start.bat`

### 推荐

**设置开机自动启动**，这样无论什么情况，系统都会自动运行，无需手动操作。

---

**设置命令**:
```batch
# 方法1: 使用批处理文件（推荐）
双击运行: setup_abu_auto_start.bat

# 方法2: 使用PowerShell
powershell -ExecutionPolicy Bypass -File "scripts\setup_abu_auto_start.ps1"
```
