# 版本管理检查清单

## ✅ 初始化前检查

### 1. 环境检查
- [ ] Git已安装（当前版本: 2.36.1 ✅）
- [ ] 已配置用户信息（`git config user.name` 和 `user.email`）
- [ ] 已检查.gitignore文件

### 2. 文件检查
- [ ] 数据库文件已排除（`*.duckdb`, `*.db`）
- [ ] 输出文件已排除（`outputs/`, 交易信号文件）
- [ ] 敏感信息已排除（`*.env`, `secrets.json`）
- [ ] 临时文件已排除（`temp/`, `*.tmp`）
- [ ] 编译文件已排除（`__pycache__/`, `target/`）

### 3. 敏感信息检查
- [ ] 没有API密钥在代码中
- [ ] 没有数据库密码在代码中
- [ ] 没有交易账户信息
- [ ] 配置文件使用环境变量或模板

---

## 🚀 初始化步骤

### 步骤1: 初始化Git仓库
```bash
git init
```

### 步骤2: 创建develop分支
```bash
git checkout -b develop
```

### 步骤3: 添加文件
```bash
git add .
```

### 步骤4: 首次提交
```bash
git commit -m "feat: 初始化青鸟交易系统项目"
```

---

## 📋 日常使用检查清单

### 提交前检查
- [ ] 代码已测试
- [ ] 没有调试代码
- [ ] 没有敏感信息
- [ ] 提交信息清晰
- [ ] 遵循提交规范

### 分支管理
- [ ] 功能开发使用独立分支
- [ ] 分支命名规范（`feature/`, `fix/`等）
- [ ] 定期同步develop分支
- [ ] 合并前进行代码审查

### 版本发布
- [ ] 所有测试通过
- [ ] 文档已更新
- [ ] 版本号已更新
- [ ] 创建发布标签
- [ ] 更新CHANGELOG

---

## 🔍 定期检查

### 每周
- [ ] 检查未提交的更改
- [ ] 同步远程仓库
- [ ] 清理已合并的分支

### 每月
- [ ] 检查.gitignore是否需要更新
- [ ] 检查是否有大文件需要排除
- [ ] 备份重要分支

---

## ⚠️ 常见问题

### 问题1: 误提交了敏感信息
**解决**:
```bash
# 从历史中删除文件
git filter-branch --force --index-filter \
  "git rm --cached --ignore-unmatch secrets.json" \
  --prune-empty --tag-name-filter cat -- --all

# 强制推送（谨慎使用）
git push origin --force --all
```

### 问题2: 误提交了大文件
**解决**:
```bash
# 使用git-lfs（如果文件很大）
git lfs track "*.duckdb"
git add .gitattributes
```

### 问题3: 需要回退提交
**解决**:
```bash
# 回退到上一个提交（保留更改）
git reset --soft HEAD~1

# 回退到上一个提交（丢弃更改）
git reset --hard HEAD~1
```

---

## 📚 参考文档

- `docs/design/版本管理建议.md` - 完整建议
- `docs/design/版本管理快速开始.md` - 快速开始
- `docs/design/版本管理总结.md` - 总结

---

**创建时间**: 2025-12-30

