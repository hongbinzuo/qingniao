# Abu系统状态与本次Session关键汇总（2026-01-18 00:38:27）

## 本次Session完成的核心工作
- 在 Gate.io 4H 模式匹配脚本中加入“时间持有偏多”计划输出，与统计口径一致（TopN 未来 24 根收益）。
- 新增事件细节函数，便于回踩/事件判定复用。
- 回测逻辑去重，避免同一突破事件被多个重叠窗口重复计数。
- 加入“回踩确认入场”的回测入口（已跑，样本极少且无优势）。
- CLI 新增时间持有计划参数与阈值设置项。

## 关键脚本与变更位置
- `scripts/abu_gate_4h_pattern_match.py`：核心统计与计划脚本
- `build_time_hold_plan`（~L422）：时间持有计划输出
- `detect_latest_event_details`（~L296）：事件细节函数
- `backtest_long_breakouts`（~L410）：去重回测逻辑
- `backtest_long_pullback`（~L506）：回踩回测逻辑
- CLI 参数（~L703）：时间持有计划与回测模式选择

## 当前统计结论（ETH 4H / Gate / 5000 根）
- 组合过滤（事件 + 硬分层）候选窗口：54
- Top20 未来 24 根后验统计：
  - win_rate 0.75
  - mean +0.01545
  - median +0.04676
  - p25 -0.0040, p75 +0.0601
- 结论：组合过滤具备更强统计可用性，适合用作“偏多 + 时间持有”而非突破/回踩入场。

## 时间持有计划（最新一次输出）
- status: ready
- direction: long
- entry_price: 3319.2
- hold_candles: 24（约 96 小时）
- stat_stop: 3305.90（基于 p25 下行分位折算，仅作统计参考）

## 回测结论
- 突破入场：去重后样本极少且全止损（无优势）。
- 回踩确认入场：样本更少，仍无优势。
- 扩大持有期/放宽止损：统计明显变差。

## 复现命令（需要网络访问 Gate.io）
```bash
python3 scripts/abu_gate_4h_pattern_match.py \
  --symbol ETH_USDT --interval 4h --total 5000 \
  --window 180 --future 24 --topk 20 --time-hold-plan
```

## 本次Session未执行事项（按要求保持未做）
- 未进行 tradeready 扫描
- 未执行 Gemini 标注/清理
- 未删除测试文件/临时脚本
- 未执行 git push

## 推荐下步（可选）
1) 加入中文可读版“时间持有计划”输出（便于直接阅读）
2) 固化更严格阈值（score / win_rate / samples）
3) 增加多周期过滤（4h + 1d）作为第二层门槛
