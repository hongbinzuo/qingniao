# 迁移验证清单

## ✅ 迁移完成

青鸟系统已成功迁移到 `C:\Users\zuoho\code\qingniao`

---

## 🔍 需要验证的项目

### 1. 路径引用检查

以下文件使用了相对路径，应该可以正常工作，但建议测试：

- [ ] `src/unified_trading_system.py` - 使用 `Path(__file__).parent.parent`
- [ ] `src/db_manager_trader.py` - 数据库路径
- [ ] `src/db_config.py` - 数据库配置路径
- [ ] `src/signal_monitor.py` - 信号监控路径

**验证方法**:
```bash
cd C:\Users\zuoho\code\qingniao
python src/unified_trading_system.py
```

### 2. 数据库连接

- [ ] 数据库文件已迁移到 `src/data/`
- [ ] 数据库连接路径正确
- [ ] 可以正常连接数据库

**验证方法**:
```bash
python src/verify_database.py
```

### 3. 模块导入

- [ ] 所有Python模块可以正常导入
- [ ] 相对导入路径正确
- [ ] 没有导入错误

**验证方法**:
```bash
python -c "import sys; sys.path.insert(0, 'src'); from generate_btc_de_signals import *"
```

### 4. 核心功能测试

- [ ] 生成交易信号功能正常
- [ ] 数据库操作正常
- [ ] 对话记录功能正常

**验证方法**:
```bash
# 测试生成信号
python src/generate_btc_de_signals.py

# 测试数据库
python src/verify_database.py
```

### 5. 配置文件

- [ ] `.gitignore` 配置正确
- [ ] `README.md` 内容正确
- [ ] 其他配置文件正常

---

## 🚀 下一步操作

### 1. 初始化Git仓库

```bash
cd C:\Users\zuoho\code\qingniao

# 配置Git用户信息（如果需要）
.\setup_git_config.ps1

# 初始化Git仓库
init_git_repo.bat
```

### 2. 安装依赖

```bash
# 确保所有依赖已安装
pip install -r requirements.txt
pip install -r requirements_db.txt
```

### 3. 运行测试

```bash
# 测试核心功能
python src/generate_btc_de_signals.py
python src/verify_database.py
```

---

## 📝 注意事项

1. **数据库文件**: 数据库文件已迁移，建议备份原数据库
2. **路径引用**: 大部分使用相对路径，应该可以正常工作
3. **遗留文件**: 已正确排除，不会影响新系统

---

**创建时间**: 2025-12-30

