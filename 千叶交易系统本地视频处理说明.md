# 千叶交易系统 - 本地视频处理说明

**创建时间**: 2025-12-30

---

## 📁 视频文件放置位置

请将您下载的视频文件放在以下目录：

```
data/chiba_videos/downloads/
```

**支持的视频格式**:
- `.webm`
- `.mp4`
- `.mkv`
- `.avi`
- `.mov`
- `.m4v`

---

## 🚀 使用方法

### 方法1: 使用默认目录

1. 将视频文件放到 `data/chiba_videos/downloads/` 目录
2. 运行处理脚本：

```bash
python src/chiba_trading_system/process_local_videos.py
```

### 方法2: 指定视频目录

```bash
python src/chiba_trading_system/process_local_videos.py --videos-dir "您的视频目录路径"
```

---

## 📋 处理流程

脚本会自动：

1. **查找视频文件** - 在指定目录中查找所有视频文件
2. **转录视频** - 使用Whisper将视频转录为文字
   - 如果已存在转录文件，会跳过转录步骤
   - 转录结果保存在 `data/chiba_videos/transcripts/`
3. **提取交易规则** - 从转录文本中提取：
   - 入场条件
   - 止损规则
   - 止盈规则
   - 风险管理
   - 技术指标
   - 图表形态
   - 市场分析
4. **保存规则** - 规则保存在 `data/chiba_videos/rules/chiba_trading_rules.json`

---

## 📝 输出文件

### 转录文件

- **JSON格式**: `data/chiba_videos/transcripts/{视频名}.json`
  - 包含完整转录结果和时间戳
- **文本格式**: `data/chiba_videos/transcripts/{视频名}.txt`
  - 纯文本转录内容

### 规则文件

- **规则文件**: `data/chiba_videos/rules/chiba_trading_rules.json`
  - 包含所有提取的交易规则
  - 规则按类型分类
  - 包含规则摘要统计

---

## ⚙️ 依赖要求

确保已安装：

```bash
pip install openai-whisper
```

**注意**: 首次运行时会自动下载Whisper模型（约72MB），可能需要几分钟。

---

## 🔄 重新处理

- 如果视频文件已转录，脚本会跳过转录步骤
- 如需重新转录，删除对应的转录文件即可
- 规则提取会基于最新的转录文件

---

## 📊 生成交易信号

规则提取完成后，运行：

```bash
python src/chiba_trading_system/chiba_trading_system.py
```

系统会根据提取的规则生成BTC、黄金、白银的交易信号。

---

## ❓ 常见问题

### Q: 转录很慢怎么办？

A: 脚本使用`tiny`模型（最快），但视频较长时仍需要时间。可以：
- 等待处理完成
- 使用更快的GPU（如果有）
- 分段处理视频

### Q: 模型下载失败？

A: 如果遇到SHA256校验失败，脚本会自动清除缓存并重新下载。

### Q: 规则提取不准确？

A: 规则提取基于关键词匹配，可能不够精确。可以：
- 手动查看转录文本
- 手动编辑规则文件
- 改进规则提取算法

---

**状态**: ✅ 准备就绪，等待视频文件








