# 高级PA扫描器 - 设计决策

## 概述
本文档概述了高级PA扫描器系统的关键设计决策，该系统支持跨多个时间框架和信号质量模式的持续本地扫描交易信号。

---

## 1. 部署策略

**决策**：本地执行而非云部署（Railway + Neon）

**理由**：
- 对执行环境有更大的控制权
- 无部署复杂性或成本
- 更容易调试和监控
- 可立即访问日志和信号
- 不依赖外部服务

**实现**：
- Python脚本在Windows本地运行
- 批处理文件便于执行
- 本地PostgreSQL数据库存储信号
- 本地文件系统存储审计报告

---

## 2. 多时间框架扫描

**决策**：同时扫描5m、15m和1h时间框架

**理由**：
- 捕捉不同交易风格的机会
- 5m：快速剥头皮和逆势机会
- 15m：平衡的波段交易
- 1h：长期持仓交易
- 提供全面的市场覆盖

**实现**：
- 每个周期顺序扫描所有三个时间框架
- 每个时间框架使用适当的上下文过滤器
- 结果存储时带有时间框架元数据

---

## 3. 币种选择

**决策**：按市值排名前20的币种

**理由**：
- 市值表明流动性和稳定性
- 降低低交易量操纵的风险
- 专注于成熟的加密货币
- 便于持续监控的可管理数量

**实现**：
- 使用 `--rank-by marketcap` 参数
- 查询CoinCap API获取排名
- 可通过 `--top` 参数配置

---

## 4. 信号质量模式

**决策**：三种不同的信号模式（激进、中性、保守）

**理由**：
- 不同交易者有不同的风险承受能力
- 允许比较信号质量与数量
- 根据结果进行优化
- 为不同市场条件提供灵活性

**模式规格**：

### 激进模式
- 最低分数：0.5
- 最低向量匹配：0.6（60%）
- 上下文过滤：关闭
- 用例：更多信号，活跃交易

### 中性模式（推荐）
- 最低分数：0.65
- 最低向量匹配：0.7（70%）
- 上下文过滤：Brooks模式
- 用例：平衡方法

### 保守模式
- 最低分数：0.75
- 最低向量匹配：0.8（80%）
- 上下文过滤：Brooks + Regime36
- 用例：仅高质量信号

---

## 5. 向量匹配过滤

**决策**：排除低概率向量匹配

**理由**：
- 向量匹配提供模式相似度分数
- 低分数表示模式对齐弱
- 过滤提高信号质量
- 减少假阳性

**实现**：
- 最低阈值因模式而异（60%、70%、80%）
- 在模式匹配阶段应用
- 记录在信号元数据中

---

## 6. 趋势跟随规则

**决策**：5m允许逆势，15m和1h仅跟随趋势

**理由**：
- 5m时间框架：快速波动可从逆势中获利
- 15m/1h时间框架：趋势跟随更可靠
- 降低长时间框架的风险
- 符合Brooks PA原则

**实现**：
- 每个时间框架设置 `allow_counter_trend` 标志
- 上下文过滤器强制趋势对齐
- 记录在扫描输出中

---

## 7. 审计报告

**决策**：每4小时生成审计报告

**理由**：
- 定期性能跟踪
- 识别模式有效性
- 支持持续改进
- 提供问责制

**实现**：
- 通过 `--audit-interval` 自动生成
- 保存为 `outputs/audit_reports/` 下的时间戳文本文件
- 包含胜率/败率统计和分周期汇总

---

## 8. 扫描间隔

**决策**：默认15分钟扫描间隔

**理由**：
- 平衡新鲜度与API速率限制
- 允许模式发展时间
- 降低计算负载
- 可根据不同需求配置

**实现**：
- 通过 `--interval` 参数配置
- 扫描周期之间休眠
- 带时间戳记录

---

## 9. 数据库存储

**决策**：本地PostgreSQL数据库

**理由**：
- 强大的关系型数据库
- 支持高精度数值（DECIMAL）
- 更好的并发支持
- 方便未来迁移到云端

**实现**：
- 数据库：PostgreSQL（通过 `PG_*` 环境变量配置）
- 表：`trading_signals`
- 包含所有信号元数据
- 支持多进程并发访问

---

## 10. 用户界面

**决策**：批处理文件 + 命令行界面

**理由**：
- 对Windows用户简单
- 无GUI复杂性
- 易于自定义
- 可脚本化和自动化

**实现**：
- 常见场景的 `.bat` 文件
- 使用argparse的Python CLI
- 全面的控制台和文件日志记录

---

## 关键优势总结

1. **灵活性**：多种模式和时间框架
2. **控制**：本地执行和存储
3. **质量**：向量过滤和趋势规则
4. **问责**：定期审计报告
5. **简单**：易于使用的批处理文件
6. **可扩展**：可配置参数

---
